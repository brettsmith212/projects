Features Needed:

To Do:

 - In Firestore Database -> Rules -> need to create authentication to write to database.  Right now it is set to if true, needs to be changed to if false.

 X Add in a pull from Firebase to Table.js

 X Add Table dashes to the summary
    Use the github of previous version to understand dashesLoaded
    Need to find a way to loop through the object (showing up as 1) 
        Maybe there is a way as an array?

Test update